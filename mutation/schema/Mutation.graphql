type Mutation {
    storeUser(
        name: String
        email: String
        age: Int
    ): User!

    updateUser(
        id: Int
        name: String
        email: String
        age: Int
    ): User

    destoryUser(id: Int): User
}