type Profile {
  id: Int
  name: String!
}

enum Status {
  ACTIVED
  BLOCKED
}

type User {
  id: ID
  name: String!
  email: String!
  age: Int
  salary: Float
  vip: Boolean
  profile: Profile
  status: Status
}
